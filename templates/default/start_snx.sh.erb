#!/usr/bin/expect

set timeout 10
spawn snx -s <%= @snx_server %> -u <%= @snx_user %>
while {1} {
  expect {
 
    eof                             {break}
    "Please enter your password:"   {send "<%= @snx_password %>\r"}
    "Do you accept"                 {send "y\r"}
  }
}