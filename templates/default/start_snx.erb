#!/usr/bin/expect

set timeout 10
spawn snx -s <%= @server %> -u <%= @user %>
while {1} {
  expect {
 
    eof                             {break}
    "Please enter your password:"   {send "<%= @password %>\r"}
    "Do you accept"                 {send "y\r"}
  }
}